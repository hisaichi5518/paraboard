require 'spec_helper'

describe Paraboard do
  it 'has a version number' do
    expect(Paraboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
